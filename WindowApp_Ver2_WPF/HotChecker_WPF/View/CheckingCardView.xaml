<UserControl x:Class="HotChecker_WPF.View.CheckingCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HotChecker_WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding EnterCommand}"/>
    </UserControl.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.4*"/>
            <RowDefinition Height="0.6*"/>
        </Grid.RowDefinitions>
        <Viewbox>
            <TextBlock Grid.Row="0" Text="바코드리더기에 카드를 인식시켜주세요!" FontSize="30" TextWrapping="Wrap" 
                   Style="{StaticResource tbDefaultStyle}" FontFamily="나눔고딕" FontWeight="Bold"/>

        </Viewbox>
        <TextBox Grid.Row="1" Width="1" Height="1" Text="{Binding BarcodeData, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
